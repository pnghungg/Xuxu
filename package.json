{
  "name": "donate-xuxu-cloudflare",
  "version": "1.0.0",
  "description": "Donate Xu Xu — Migrated from Google Apps Script to Cloudflare Workers + Cloudflare Pages",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "pages:deploy": "wrangler pages deploy pages --project-name=donate-xuxu"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
